'''Agent class'''

class Agent(object):
    '''Agent'''

    def __init__(self, x, y):
        self.x = x
        self.y = y
        self.position = (self.posx, self.posy)
        self.velocity = (self.x, self.y)
        self.heading = 

